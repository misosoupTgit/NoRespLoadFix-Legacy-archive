# NoRespLoadFix

A mod that fixes an issue where, when launching resource-intensive setups such as modpacks,
Windows determines that the system is unresponsive during loading and displays a dialog asking whether to terminate the process.